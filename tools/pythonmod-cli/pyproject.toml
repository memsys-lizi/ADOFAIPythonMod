[project]
name = "pythonmod-cli"
version = "0.1.0"
description = "Developer CLI for PythonMod ADOFAI mods"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pythonmod = "pythonmod_cli.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
